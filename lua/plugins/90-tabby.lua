return {
    {
        'TabbyML/vim-tabby',
        config = function()
            vim.g.tabby_trigger_mode = 'manual'
        end
    },
    -- lazy = true,
    -- config = function()
        --     --- Add config here. Example config:
        --     -- vim.g.tabby_keybinding_accept = '<Tab>'
        -- end,
        -- cmd = "Tabby"
}
